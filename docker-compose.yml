services:
  redis:
    image: redis:alpine
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
  worker:
    build: .
    depends_on:
      - redis
    command: python worker.py